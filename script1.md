bash --version #ver version de bash
echo $SHELL #verificar que el shell default
echo $PATH #contiene una lista de directorios donde el shell debe buscar comandos que puedan ejecutar cuando escribimos un nombre de comando
env #echar un vistazo a todo el entorno de valores en una sesion determinada
